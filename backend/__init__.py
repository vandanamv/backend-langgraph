"""Backend package for LangGraph.

This file makes the `backend` directory an explicit Python package.
It can be left empty, but having it avoids issues with older tooling
or environments that don't rely on implicit namespace packages.
"""

__all__ = ["app"]
